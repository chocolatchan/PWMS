import 'package:flutter/material.dart';

class PackContainerScreen extends StatelessWidget {
  const PackContainerScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Pack Container')), body: const Center(child: Text('Pack Container Screen')));
}
