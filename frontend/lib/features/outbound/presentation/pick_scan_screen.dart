import 'package:flutter/material.dart';

class PickScanScreen extends StatelessWidget {
  const PickScanScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Pick Scan')), body: const Center(child: Text('Pick Scan Screen')));
}
