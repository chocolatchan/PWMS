import 'package:flutter/material.dart';

class IotAlertsScreen extends StatelessWidget {
  const IotAlertsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('IoT Alerts')), body: const Center(child: Text('IoT Alerts Screen')));
}
