import 'package:flutter/material.dart';

class QCSubmitScreen extends StatelessWidget {
  const QCSubmitScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Submit QC')), body: const Center(child: Text('QC Submit Screen')));
}
